import 'package:flutter/material.dart';

class WHColors {
  // Primary Colors
  static const Color primary = Color(0xFF1E88E5); // Biru profesional
  static const Color primaryDark = Color(0xFF1565C0);

  // Background Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;

  // Status Colors
  static const Color success = Color(0xFF4CAF50); // Stok Aman
  static const Color warning = Color(0xFFFF9800); // Stok Menipis
  static const Color error = Color(0xFFD32F2F); // Zona Penuh / Error

  // Neutral Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color grey = Color(0xFFBDBDBD);
}
